language: node_js
node_js:
  - "lts/*"
branches:
  only:
  - master
cache:
  directories:
    - node_modules
script:
    - ./deploy.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  verbose: true
  on:
    branch: master